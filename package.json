{
  "name": "invoice-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Fast, offline-capable invoice generator for Eng Hoon Residences.",
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node --check tests/app.test.js",
    "test": "npm run check && node --test tests/app.test.js"
  },
  "dependencies": {
    "html2pdf.js": "^0.14.0"
  }
}
